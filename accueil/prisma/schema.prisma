generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model DataStore {
  key       String   @id
  data      String
  updatedAt DateTime @updatedAt
}
